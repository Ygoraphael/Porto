package fme;

import java.util.Vector;
import javax.swing.JComboBox;



















































































































































































































































































































































































































































































































































































































































































class CNUTS_II_Continente
  extends CTabela
{
  CTabela base;
  JComboBox j;
  
  CNUTS_II_Continente(CTabela _base)
  {
    base = _base;
    name = (base.name + "2");
    
    cCol = 0;
    dCol = 1;
    










    data_table = new Vector();
    
    for (int i = 0; i < base.data_table.size(); i++) {
      String[] item = (String[])base.data_table.elementAt(i);
      if (!item[0].matches("20|30")) { data_table.addElement(item);
      }
    }
    j = null;
  }
}
