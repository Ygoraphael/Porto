package fme;

import java.util.regex.Matcher;
import java.util.regex.Pattern;
import javax.swing.JOptionPane;


















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































class CFType_Email
  extends CFType
{
  String v;
  
  CFType_Email() {}
  
  boolean setText(String _v, boolean interact)
  {
    v = _v;
    

    if (_v.length() == 0) {
      return true;
    }
    String erro = "E-mail inválido.";
    




















    String expression = "^[\\w\\.-]+@([\\w\\-]+\\.)+[A-Z]{2,}$";
    Pattern pattern = Pattern.compile(expression, 2);
    Matcher matcher = pattern.matcher(_v);
    if (!matcher.matches()) {
      if (interact) {
        JOptionPane.showMessageDialog(null, erro, "Erro", 
          0);
      }
      return false;
    }
    return true;
  }
  
  boolean setString(String s) {
    v = s;
    return true;
  }
  
  String getText() {
    return v;
  }
  
  String getString() {
    return v;
  }
}
