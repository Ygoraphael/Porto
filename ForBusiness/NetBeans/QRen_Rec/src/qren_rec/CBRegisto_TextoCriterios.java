package fme;

import java.util.Vector;










































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































class CBRegisto_TextoCriterios
  extends CBRegisto
{
  public String getPagina()
  {
    return tag;
  }
  
  String titulo = "";
  boolean obrigatorio = true;
  
  CBRegisto_TextoCriterios(String _tag, String _titulo) {
    tag = _tag;
    titulo = _titulo;
    
    Frame_TextoCriterios P = (Frame_TextoCriterios)fmeApp.Paginas.getPage(tag);
    if (P == null) return;
    started = true;
    
    Campos.add(new CHCampo_TextArea("texto", P.getJTextArea_Txt(), this));
  }
  
  CHValid_Grp validar(CHValid_Grp err_list) {
    if (err_list == null) {
      err_list = new CHValid_Grp(this, titulo);
    }
    extract();
    String erro = "Texto demasiado extenso. Por favor, abrevie até 32000 caracteres.";
    
    if ((getByName("texto").isEmpty()) && (obrigatorio))
      err_list.add_msg(new CHValid_Msg("texto", "%o"));
    if ((!getByName("texto").isEmpty()) && (getByName"texto"v.length() > 32000)) {
      err_list.add_msg(new CHValid_Msg("texto", erro));
    }
    return err_list;
  }
}
