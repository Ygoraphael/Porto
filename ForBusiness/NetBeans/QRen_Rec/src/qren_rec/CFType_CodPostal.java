package fme;

import javax.swing.JOptionPane;
























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































class CFType_CodPostal
  extends CFType
{
  String v;
  
  CFType_CodPostal() {}
  
  boolean setText(String _t, boolean interact)
  {
    v = _t;
    if (v.length() == 0) {
      return true;
    }
    boolean ok = _lib.exact_match(_t, "####-###");
    if (ok) {
      if (Integer.parseInt(v.substring(0, 4)) < 1000)
        ok = false;
      if (Integer.parseInt(v.substring(5, 8)) == 0)
        ok = false;
      if ((!ok) && 
        (interact)) {
        JOptionPane.showMessageDialog(null, "Código Postal inválido.", "Erro", 
          0);
        return ok;
      }
    }
    
    if ((!ok) && 
      (interact)) {
      JOptionPane.showMessageDialog(null, "Código Postal incorreto.\nPor favor, introduza o Código Postal no formato \"nnnn-nnn\".", 
        "Erro", 
        0);
    }
    
    return ok;
  }
  
  boolean setString(String s) {
    v = s;
    return true;
  }
  
  String getText() {
    return v;
  }
  
  String getString() {
    return v;
  }
  
  boolean acceptKey(char c) {
    if (Character.isDigit(c)) {
      return true;
    }
    if (c == '-') {
      return true;
    }
    return false;
  }
}
